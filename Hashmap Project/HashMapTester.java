import java.util.ArrayList;

public class HashMapTester
{
	public static void main(String[] args) 
	{
		/*
		To get you started, here is code that will allow you to test
		your toString() method before having to complete the put() method

		The expected output of the code below has been attached to the 
		assignment. Once you can match the expected output, move on 
		to the put() method next.
		*/
		// HashMap<Integer, Integer> testMap = new HashMap<>();

		// ArrayList<Node<Integer, Integer>> mapList = testMap.getBackingList();

		// Node<Integer, Integer> node1 = new Node<>(1, 0);
		// Node<Integer, Integer> node7 = new Node<>(7, 0);
		// Node<Integer, Integer> node11 = new Node<>(11, 0);
		
		// node1.setNext(node11);

		// mapList.set(1, node1);
		// mapList.set(7, node7);

		// System.out.println(testMap);

	}
}